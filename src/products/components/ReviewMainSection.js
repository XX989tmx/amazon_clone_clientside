import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const ReviewMainSection = (props) => {
  return (
    <Col xs={12} md={9} lg={9}>
      <Row>
        <Col xs={12}>レビュー一覧</Col>
      </Row>
    </Col>
  );
};

export default ReviewMainSection;
