import React from 'react';
import { Link } from 'react-router-dom';

const GrandGrandChildCategoryIndexItem = (props) => {
    return (
      <div>
        {/* <Link to={`/product/${props.id}`}>
          {" "}
          <div>
            {" "}
            {props.name}
            {props.price}
          </div>
        </Link> */}
      </div>
    );
}

export default GrandGrandChildCategoryIndexItem;
