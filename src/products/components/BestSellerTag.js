import React from "react";
import reactBootstrap, { Container, Row, Col, Button } from "react-bootstrap";
const BestSellerTag = (props) => {
  return (
    <Row>
      <Col>best seller tag</Col>
    </Row>
  );
};

export default BestSellerTag;
