import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const CartItemTotalCount = (props) => {
  return (
    <Row>
      <Col>n点の商品がカートに入っています</Col>
    </Row>
  );
};

export default CartItemTotalCount;
