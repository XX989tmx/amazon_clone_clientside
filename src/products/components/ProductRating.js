import React from "react";
import reactBootstrap, { Container, Row, Col, Button } from "react-bootstrap";
const ProductRating = (props) => {
  return <Col xs={5}>reviews</Col>;
};

export default ProductRating;
