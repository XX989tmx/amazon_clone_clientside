import React from "react";
import reactBootstrap, { Container, Row, Col, Button } from "react-bootstrap";
const ProductRatingCount = (props) => {
  return <Col xs={7}>count</Col>;
};

export default ProductRatingCount;
