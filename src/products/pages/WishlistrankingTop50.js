import React from 'react';

const WishlistrankingTop50 = () => {
  //Backend:  totalNumberOdTimesOfAddedToWishlist filedを新たに追加し、ウィッシュリストに追加されるたびに＋１する。各カテゴリーレベルごとに、このフィールドが多い順ソートし、先頭５０個を受け取る。
  // await Product.find({categories:categories}).sort({totalNumberOdTimesOfAddedToWishlist: -1}).limit(50);
  return <div>WishlistrankingTop50</div>;
}

export default WishlistrankingTop50;
