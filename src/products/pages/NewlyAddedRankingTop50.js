import React from "react";

const NewlyAddedRankingTop50 = () => {
  //  backend logic:
  // await Product.find({categories:categories}).sort({dateAdded??: -1}).limit(50);
  return (
    <div>
      <h1>NewlyAddedRankingTop50</h1>
    </div>
  );
};

export default NewlyAddedRankingTop50;
