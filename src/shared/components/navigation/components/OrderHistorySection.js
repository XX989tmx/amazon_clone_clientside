import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const OrderHistorySection = (props) => {
  return <Col xs={3}>注文履歴</Col>;
};

export default OrderHistorySection;
