import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const ReturnOrderedItem = (props) => {
  return (
    <Row>
      <Col>
        <Button>商品の返品</Button>
      </Col>
    </Row>
  );
};

export default ReturnOrderedItem;
