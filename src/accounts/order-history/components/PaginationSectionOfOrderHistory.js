import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const PaginationSectionOfOrderHistory = (props) => {
  return (
    <Row>
      <Col>pagination</Col>
    </Row>
  );
};

export default PaginationSectionOfOrderHistory;
