import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const TotalPriceParagraph = (props) => {
  return (
    <Row>
      <Col>合計</Col>
    </Row>
  );
};

export default TotalPriceParagraph;
