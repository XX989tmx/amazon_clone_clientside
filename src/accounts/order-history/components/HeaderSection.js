import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const HeaderSection = (props) => {
  return <Col md={6}>注文履歴 header</Col>;
};

export default HeaderSection;
