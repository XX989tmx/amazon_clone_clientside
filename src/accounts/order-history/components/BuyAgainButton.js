import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const BuyAgainButton = (props) => {
  return (
    <Row>
      <Col>buy again button</Col>
    </Row>
  );
};

export default BuyAgainButton;
