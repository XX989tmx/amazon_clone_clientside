import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const AddToCartButtonOfBuyAgainItem = (props) => {
  return (
    <Row>
      <Col>
        <Button>カートに入れる</Button>
      </Col>
    </Row>
  );
};

export default AddToCartButtonOfBuyAgainItem;
