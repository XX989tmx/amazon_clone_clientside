import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const CheckDeliveryStatus = (props) => {
  return (
    <Row>
      <Col>
        <Button>配送状況を確認</Button>
      </Col>
    </Row>
  );
};

export default CheckDeliveryStatus;
