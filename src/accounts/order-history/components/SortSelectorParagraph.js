import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const SortSelectorParagraph = (props) => {
  return <Col>に確定されたN件の注文</Col>;
};

export default SortSelectorParagraph;
