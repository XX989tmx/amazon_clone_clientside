import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const SearchBarSection = (props) => {
  return <Col md={6}>search bar</Col>;
};

export default SearchBarSection;
