import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const WriteProductReview = (props) => {
  return (
    <Row>
      <Col>
        <Button>商品レビューを書く</Button>
      </Col>
    </Row>
  );
};

export default WriteProductReview;
