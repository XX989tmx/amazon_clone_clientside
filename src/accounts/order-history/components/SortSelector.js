import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const SortSelector = (props) => {
  return <Col>sort selector</Col>;
};

export default SortSelector;
