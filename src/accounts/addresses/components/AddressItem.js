import React from 'react';

const AddressItem = (props) => {
    return (
        <div>
            <button>edit</button>
            <button>delete</button>
        </div>
    );
}

export default AddressItem;
