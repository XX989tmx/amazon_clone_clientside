import React from 'react';

const OrderComplete = () => {
    // useeffectで直近のOrderデータをfetch
    return (
        <div>
            order complete
        </div>
    );
}

export default OrderComplete;
