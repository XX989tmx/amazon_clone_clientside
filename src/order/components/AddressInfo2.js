import React from "react";
import reactBootstrap, { Col, Container, Row, Button } from "react-bootstrap";
const AddressInfo2 = (props) => {
  return (
    <Row>
      <Col>addressinfo2</Col>
    </Row>
  );
};

export default AddressInfo2;
