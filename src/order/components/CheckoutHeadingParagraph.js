import React from "react";
import reactBootstrap, { Col, Container, Row, Button } from "react-bootstrap";
const CheckoutHeadingParagraph = (props) => {
  return (
    <Row>
      <Col>purchase confirmation paragraph</Col>
    </Row>
  );
};

export default CheckoutHeadingParagraph;
