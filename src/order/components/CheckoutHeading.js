import React from "react";
import reactBootstrap, { Col, Container, Row, Button } from "react-bootstrap";
const CheckoutHeading = (props) => {
  return (
    <Row>
      <Col>注文内容を確認変更する</Col>
    </Row>
  );
};

export default CheckoutHeading;
