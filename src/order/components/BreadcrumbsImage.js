import React from "react";
import reactBootstrap, { Col, Container, Row, Button } from "react-bootstrap";
const BreadcrumbsImage = (props) => {
  return (
    <Row>
      <Col>bread crums image</Col>
    </Row>
  );
};

export default BreadcrumbsImage;
