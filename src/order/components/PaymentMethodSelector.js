import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const PaymentMethodSelector = (props) => {
  return (
    <Row>
      <Col>payment selector</Col>
    </Row>
  );
};

export default PaymentMethodSelector;
