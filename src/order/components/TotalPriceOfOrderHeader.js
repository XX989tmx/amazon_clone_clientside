import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const TotalPriceOfOrderHeader = (props) => {
  return <Col>商品の小計</Col>;
};

export default TotalPriceOfOrderHeader;
