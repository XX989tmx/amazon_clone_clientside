import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const ShipmentCostOfOrderSection = (props) => {
  return (
    <Row>
      <Col>配送料手数料</Col>
      <Col>cost</Col>
    </Row>
  );
};

export default ShipmentCostOfOrderSection;
