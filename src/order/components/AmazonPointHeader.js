import React from "react";
import reactBootstrap, { Row, Col, Container, Button } from "react-bootstrap";
const AmazonPointHeader = (props) => {
  return (
    <Row>
      <Col>Amazon points</Col>
    </Row>
  );
};

export default AmazonPointHeader;
